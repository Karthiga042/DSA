int[] arr = {0, 1, 0, 3, 12};

int slow = 0;

for (int fast = 0; fast < arr.length; fast++) {
    if (arr[fast] != 0) {
        arr[slow] = arr[fast];
        slow++;
    }
}

// fill remaining with zeros
while (slow < arr.length) {
    arr[slow] = 0;
    slow++;
}
